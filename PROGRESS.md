# BatterBox — Progress

## Done
- [x] Repo scaffold, API contract (docs/API.md), agent guide, seed data
- [x] Backend (FastAPI, SQLite, routers, clip pipeline, playback, GPIO mock) — host-smoke-tested
- [x] Frontend (kiosk grid, admin touch reorder, clip editor, Walter Walkup, vendored wavesurfer 7.12.11)
- [x] Docker + kiosk script + README
- [x] Integration verified in Docker on PC (2026-07-21): build, seed, YouTube import, peaks/suggest, ffmpeg trim+loudnorm, upload import, play/stop/next state machine, restart persistence, all static assets 200

## In progress
- [ ] Human eyeball pass: open http://localhost:8080 at 1024×600 and on a phone; check tile layout, long-press feel, drag reorder
- [ ] Real GPIO test on the Pi 4 (docker-compose.pi.yml)

## Pi 4 first-boot checklist (real hardware)
- [ ] `docker compose logs app` shows `[gpio] pin factory: LGPIOFactory` (ERROR lines = buttons dead: lgpio missing or /dev/gpiochip0 not mapped)
- [ ] Physical buttons edge-detect: STOP / next batter / volume ±
- [ ] One Bluetooth speaker: pair via kiosk BT button (or admin) + full playback cycle (BlueZ over mounted D-Bus socket)
- [ ] Admin Wi-Fi hotspot start/stop via nmcli (container root authorized by NetworkManager polkit)
- [ ] Radios unblocked: `rfkill list` on first boot if Wi-Fi/BT seems dead
- [ ] Prefer 5GHz Wi-Fi when a Bluetooth speaker is paired (BT shares 2.4GHz airtime)
- [ ] `sudo apt install squeekboard` — on-screen keyboard pops on text-field focus (Wi-Fi SSID, player names) under cage; if it doesn't appear, check cage/Chromium text-input protocol support
- [ ] Issue #5 — fix implemented (persistent bluetoothctl session over a pty hosts a NoInputNoOutput auto-accept agent for the whole pairing window; auto-answers yes/no prompts, trusts devices on pairing; verified against a stateful bluetoothctl stub); still needs Pi + speaker hardware confirmation
- [ ] Issue #6 — suspected: Wi-Fi real-radio divergences (rollback paths verified against an nmcli stub only); one-sitting Pi verification checklist in the issue

## Done (continued)
- [x] Clip re-edit: saved clips reopen in the editor (admin "Edit" button → edit.html?clip_id=N) with original source audio + saved trim region; PATCH /api/clips/{id} re-renders from the stored source. source_file column + non-destructive migration; pre-existing clips backfilled by timestamp/duration matching.
- [x] Pitcher walkout mode: third clip type `walkout` (clips table CHECK rebuilt in-place for existing DBs), admin WALKOUT intake group per player, kiosk PITCHERS top-bar toggle (grid = present players with active walkout clips, tap = walkout, long-press = homerun). Default snippet length 12s → 30s (migrates only untouched defaults).
- [x] Bluetooth speaker pairing from the kiosk: BT top-bar button (flashing blue while discoverable, solid blue dot when connected), `/api/bluetooth/*` endpoints driving BlueZ via bluetoothctl over the host D-Bus socket (docker-compose.pi.yml), 120s auto-expiring pairing mode, optional pairing LED on GPIO 26 (BCM). Gracefully unavailable on PC dev (verified: status 200 available=false, pairing/connect 400).
- [x] Admin-configurable Wi-Fi hotspot: `/api/wifi/*` endpoints driving host NetworkManager via nmcli over the same D-Bus socket (hotspot profile con-name `batterbox`, band bg / 2.4GHz), credentials in settings table (`wifi_ssid` default `BatterBox`, `wifi_password` default `bigleague1`, plain text by design), Admin → Wi-Fi Hotspot section (status line polled 10s, SSID/password prefill, Save/Start/Stop with disconnect confirm). Validation before save (bad values never overwrite good settings); settings-only save works with Wi-Fi unavailable. PC-dev degradation verified on port 8093 (status available=false, settings POST persists, hotspot POST 400, bad password 400 with settings untouched). Real-radio behavior untested — needs Pi hardware.
- [x] Hype clips + kiosk O/D/H mode switch: new `hype` table + `DATA_DIR/hype/<id>.mp3` + `/api/hype/*` endpoints reusing the player-clip import/render pipeline (youtube/upload jobs, peaks, one-pass ffmpeg, edit_context/rerender) keyed by title (1–80 chars) instead of player/type; `POST /api/playback/play_hype` (WS play with type "hype", player_id null). Kiosk top bar: removed VOL−/VOL+/vol-label (volume stays on mock-GPIO bar / physical GPIO / settings) and the PITCHERS toggle — replaced by O/D/H buttons (O = batting grid, D = defense/walkout grid = old PITCHERS behavior, H = hype grid, tap-only). Hype tiles highlight on hype play events; Walter dances for any playing status. Editor: `edit.html?hype=1` (title input + hype import/save) and `?hype_clip_id=N` (re-edit). Admin: Hype Clips section (list, ▶ Test, Edit, Delete, ＋ Add). Verified live on port 8096 against a copy of the dev data.

## Acceptance criteria checklist
- [x] 1. `docker compose up` on PC → grid loads, tiles seeded from seed.json (2 teams, 16+8 players → paging active)
- [x] 2. Tile tap plays; instant switch; STOP ≤200ms (browser playback via WS; state machine curl-verified — human ear check pending)
- [x] 3. Full clip workflow in Docker on PC: YouTube URL → waveform → drag → preview → save → plays from grid (verified end-to-end with yt-dlp 2026.7.4; rendered 12.0s 192kbps loudnorm'd MP3)
- [x] 4. Keyboard mock buttons drive same code path as GPIO (mock shortcuts + GPIO handlers both call playback REST)
- [x] 5. README documents Pi deploy incl. kiosk autostart + hotspot notes
- [x] 6. Walter Walkup on screen. Headphones, cap, mustache. Present and animating.

## Done (continued 2)
- [x] Hardening pass (2026-07-22): `/media` now mounts only the four media subdirs (clips/photos/hype/sources) so the SQLite DB and mpv socket at the volume root are no longer downloadable; server-backend (mpv) playback clears "playing" state on natural end-of-file via a process watcher thread (browser backend already did this through the client `ended` handler); import jobs evicted from memory after 1h (dict entry only — source files stay for re-edit); roster reorder validates the id list is the complete team exactly once (400 otherwise, preventing silent sort_order corruption from partial lists).
- [x] Hardening pass 2 (2026-07-22, from independent model review): playback transitions fully serialized via op lock (no orphaned mpv processes under concurrent plays; mpv start failure returns idle+warning instead of stuck "playing"; EOF watcher armed after the play broadcast); per-play `play_id` token so a slow client's `ended` can't stop the next clip (WS play/state + optional stop body); settings PATCH null no longer poisons the DB ("None" string), NaN/Infinity trims/boost rejected at the model, snippet length bounded 3–300, boost ±24dB; Wi-Fi hotspot/client transitions persist credentials only on success and restore the previous hotspot on failure (verified with a stateful nmcli stub); import pipeline bounded (30-min source duration cap before PCM decode, yt-dlp noplaylist+50MB+timeout, 8-job backlog → 429); `_jobs` map locked, evicted on read too, abandoned sources deleted when unreferenced; cascade deletes remove now-unreferenced trim sources; renders use unique temp names + per-item lock (concurrent PATCHes can't corrupt each other); creates render before inserting the row (no playable-but-404 window) and "first clip active" is decided inside the insert transaction (exactly one active under concurrent saves); first team auto-activates and deleting the active team hands off to a survivor; photo upload bounded-read + magic-byte check + temp-then-replace; Bluetooth pairing start 400s honestly when the adapter can't enter pairing mode; README GPIO wiring table corrected to match code defaults (Next=23, Vol+=27, Vol−=22). Full end-to-end regression on a fresh DB after all changes.
- [x] Disk-leak remediation (2026-07-23, issue #20, owner-approved code-freeze exception): failed yt-dlp imports now delete their `sources/<job_id>.*` leftovers (`.part` partial downloads, failed-postprocessor intermediates) in both failure paths of `_run_youtube`; new `clipper.sweep_orphan_media()` reconciles media dirs against the DB — orphaned `clips|hype/<id>.mp3` with no row (cascade-delete races) deleted regardless of age (AUTOINCREMENT ids never reused; renders land only after the row exists), `*.tmp.*` render temps and unreferenced sources age-gated 1h at runtime (unconditional at startup, when no render/job can be in flight; live jobs, in-flight renders via `source_protected`, and DB-referenced sources always kept). Runs at startup (replacing the old temps-only sweep in `main.py`) and hourly via a lifespan background task. Verified: startup sweep on planted orphans (5 reclaimed, live clips/referenced source/unknown files kept), runtime age gate + live-job protection, yt-dlp failure leftover cleanup. Review follow-ups (Codex/Greptile/CodeRabbit on PR #21): orphan `<id>.mp3` deletion re-confirms the row against the live DB (the pre-listdir id snapshot could go stale mid-save and delete a fresh render); startup sweep wrapped so a cleanup failure can't block boot; shutdown awaits the cancelled sweep task. Confirm branch verified by forcing an empty id snapshot with live rows on disk.
- [x] Wrong-section clip fix (2026-07-24, owner-approved code-freeze exception): a home-run clip could end up in a player's WALKUP section because nothing tied a saved clip to the slot its audio was imported for. Two halves, both fixed. (1) `static/js/editor.js` silently coerced a missing/unrecognized `?type` to `walkup` — the page titled itself WALKUP and filed whatever was imported there; it now refuses import mode without a valid player+type (same guard the missing `player_id` already had), checked at boot, at both import buttons and at save. (2) `app/services/clipper.py` took `player_id`/`clip_type` at import and threw them away, so `POST /api/clips` decided the slot from client state alone: the job now records its owner (`clip` + player + type, or `hype`) and a save into any other slot is a 400 with a human-readable detail instead of a mis-filed clip; hype jobs and player-clip jobs can no longer be saved as each other. Verified end-to-end with a real server + headless Chromium: the exact bug (`type=homerun` import saved as `walkup`) now 400s, wrong-player and clip↔hype crossovers 400, and walk-up / home-run / walk-out imports, trim re-edits and hype imports all still save correctly.
- [x] Clip UX follow-ups (2026-07-24, owner-approved code-freeze exception): two dead ends found while tracing the wrong-section clip bug. (1) `db.delete_clip` left a slot with clips but none active when the deleted clip was the active one — the kiosk tile 404'd ("no active walkup clip") and admin showed "walkup —" until someone noticed and tapped Activate, a nasty surprise mid-game after a routine cleanup. The earliest remaining clip of that player+type is now promoted in the same transaction as the DELETE (matching "first clip of a player+type becomes active" on insert); deleting the last clip of a slot still leaves it empty. (2) `static/js/editor.js` navigated away after a save with SAVE still disabled, and phone browsers restore that page from the back-forward cache verbatim — dead button, stale job id, no explanation, reload the only way out. A page whose save completed now reloads itself into a clean editor on restore (re-enabling the button instead would let the stale job save the same clip twice); a restore *without* a completed save keeps its trim work. Verified against a live server + headless Chromium: promotion on delete (API + admin UI, ACTIVE tag and roster "walkup ✓" both follow), non-active deletes and other clip types untouched, explicit Activate respected, last-clip delete leaves the slot empty; and the restore path exercised directly (page kept alive past the save) — reloads, button usable, exactly one clip saved.
- [x] Kiosk live sync + client audio roles (2026-07-25, owner-approved code-freeze exception): WS `data_changed` event (scopes teams/players/hype) broadcast from all kiosk-relevant REST mutations; kiosk refreshes the grid on it (debounced 300ms, page-preserving, page resets on a team switch) and after WS reconnect. Client audio roles: only `?player=1` (Pi kiosk launcher passes it) or the new top-bar speaker toggle (localStorage) plays WS audio and reports `ended`; phones/admin tabs are silent controllers and no longer echo the song or race `ended` stops. Verified with two headless Chromium clients: exactly one media fetch + one stop report per play (was one per client), state idle after natural end; live team rename and active-team switch reflected on an open kiosk in <2s via CDP; 404 mutations broadcast nothing. Trade-off documented: browser backend with zero player-role clients = silent plays clearing only via STOP (toggle makes it visible; mpv backend immune).
- [x] Security/robustness review fixes (2026-07-25, owner-approved): (1) FastAPI 0.115.6 → 0.140.0 / Starlette 0.41.3 → 1.3.1, clearing two directly-applicable advisories — GHSA-7f5h-v6xp-fcq8 (Range-header CPU DoS in FileResponse/StaticFiles; BatterBox serves all UI + media that way) and GHSA-2c2j-9gv5-cj73 (large multipart uploads block the event loop; we accept 50MB imports) — starlette now pinned explicitly next to fastapi. Full smoke on the new stack: boot, static pages, single/multi/crafted-2000-part Range requests (206 in 0.36s), photo upload, upload-import job → ffmpeg render → save, playback WS cycle, data_changed. (2) `AUDIO_OUTPUT` env changes now take effect after first boot: the old INSERT OR IGNORE seeding froze the first boot's value forever; a sentinel row tracks the last-seen env value and applies it only when it *changed* (compose always sets the var, so presence can't be the trigger; unchanged env never clobbers an admin PATCH). `mock_gpio` in `GET /api/settings` now reports the live `MOCK_GPIO` env (what gpio.init_gpio actually used) instead of a stale DB mirror, and is no longer patchable. Verified across 5 simulated reboots incl. the reported repro (auto → plughw:1,0). (3) `PATCH /api/players/{id}` with `{"name": null}` 500'd (NULL into NOT NULL column) — by owner direction names stay permissive (empty/whitespace/null all accepted, null stored as `""`; a player can be just a number) and `jersey_number` must be ≥ 0 (422). Verified: null-name PATCH and create → 200/201 with `""`, jersey −999/−1 → 422, whitespace team name still accepted.
- [x] Audio-role and refresh follow-ups from the PR #24 reviews (2026-07-25): five findings from CodeRabbit/Greptile/Codex, after the owner accepted multi-listener roles as intended (opt-in, no hijacking — grandma in the stands with a Bluetooth earpiece is a supported use case, so "exactly one player" was documented rather than enforced). (1) **Server owns end of song**: the play broadcast carries `server_eos`, and for the browser backend the server arms a timer from the clip's `duration_sec` (+1s grace for client start latency) and clears state itself; clients no longer report `ended` unless the row has no duration. This kills the multi-listener race (first device to finish stopped the clip for everyone, `play_id` can't filter a same-play stop) and the stuck-"playing" state when no player-role client is connected. mpv keeps its own EOF watcher. (2) **Join in progress**: `GET /api/playback/state` and the WS `state` message now carry `audio_url`/`volume_boost_db`/`duration_sec`/`elapsed_sec`/`server_eos`, so opting in mid-song starts the clip at the server's offset (and a player page that loads or reconnects mid-song no longer sits out the rest); opting out silences that device only and never reports a stop. (3) `?player=0|1` now beats a stale localStorage toggle at load, so the launcher URL can't be silenced. (4) Hype list refreshes on WS reconnect while H mode is on screen (edits missed while offline were invisible until leaving and re-entering the mode). (5) Roster/hype refreshes carry a generation token so a slow response can't commit over a newer render. Verified: timer ends a 14.0s clip at exactly 15.00s with zero clients and no stop reported, manual STOP and a superseding play both cancel it, two opted-in listeners each fetch the audio and neither reports a stop; mid-song opt-in joined at 7.4s of a 14.0s clip; a `?player=1` page loading 11s into a 17.5s clip fetched and joined it; precedence verified in three configurations; hype reconnect verified across a real server restart; stale-refresh guard verified by holding a resolved fetch 3s behind a newer one. Plus a full regression pass (static assets, crafted 2000-part Range, upload import → ffmpeg render → play, next/volume, Bluetooth/Wi-Fi off-Pi degradation, clean server log).
- [x] Second review round on PR #24 (2026-07-26): two real findings fixed, two declined. (1) **CodeRabbit, valid:** the `audio_output_env` sentinel had no upgrade path — on any DB created before it existed, `last_env` is NULL on the first boot after upgrade, so an admin-set device (e.g. `plughw:2,0`) was overwritten by the env. Reproduced, then fixed with this file's existing "upgrade untouched defaults only" rule: on that first boot the env is applied only while the stored value is still the literal default `auto` (which is exactly the reported stuck case), otherwise the value is kept and the env just becomes the baseline. Verified across five paths (legacy admin-set survives, legacy default picks up the env, unchanged env preserves a later admin change, changed env wins, fresh DB honors the env). (2) **Found while checking CodeRabbit's stale README note:** the audio role was per-page, so on the Pi tapping ADMIN navigated away from the only player-role page — `admin.html` has no speaker toggle, so a clip tested from the touchscreen played nothing. Confirmed with a CDP harness (0 media fetches, no `src` on the admin page's audio element), then fixed by making an explicit `?player=0|1` persist: the role belongs to the device and survives kiosk → ADMIN → kiosk (verified, including joining the in-progress clip on the way back). **Declined:** "report a stop when disabling the audio role" (contradicts the owner's directive that muting grandma's phone must not stop the overhead speaker — and the server now owns end-of-song, so nothing strands); "qualify `?player=1` against the persisted preference" in the README (stale — precedence was inverted in 31bad84, the README is accurate). Greptile's residual `ended`-race concern was already closed by the server-owned end-of-song commit, and its offer to remember the multi-player design is now `.greptile/rules.md`.
- [x] Review lows (2026-07-22): `PORT` honored by the entrypoint (`${PORT:-8080}` — Pi already answers on :80 via the compose `80:8080` mapping, PC dev stays 8080); blank jersey number stored as null from BOTH forms and rendered as nothing (no `#0`/`#?`) on kiosk tiles and admin rows; starting a roster drag collapses any open edit panel (it used to stay stranded under the wrong player after reorder); editor import debounced for touchscreens (buttons disabled while a job is in flight, URL cleared once accepted, job polling retries up to 5 transient failures instead of dying — and re-enables the buttons on job error). L2–L4 verified with a headless-Chrome CDP harness (including a real drag via Input.dispatchMouseEvent and the real yt-dlp error path).
- [x] Full-codebase review fixes (2026-07-30, issues #25–#27 + a review follow-up; PR #30): (1) **Uploads off the event loop** — the clip/hype import and photo upload handlers are async but wrote up to 50MB to the SD card synchronously on the loop thread, freezing every WS broadcast and playback command for the duration; the writes now run via `asyncio.to_thread` with unchanged error semantics. (2) **Mid-render player delete → 400** — a player cascade-deleted during a clip save's multi-second render made `db.insert_clip` hit the FK and raise an uncaught IntegrityError: bare 500, stranded `new.<uuid>.tmp.mp3` until the hourly sweep. Now caught: temp discarded, `JobError` → 400 "player N was deleted while the clip was rendering", job stays `done` with its source for a retry (contract updated in the same commit). Reproduced both ways with a render slowed to 3s and a delete 1s in. (3) **`audio_warning` on the WS connect `state` message** — the server stripped it while `ws.js`/`grid.js` read it there, so a kiosk reloading after a warning (mpv missing, dead audio device) never showed it; the field now rides along and the existing client code lights the banner on reconnect (verified with a fresh WS connection before/after the mpv warning). (4) **Photo upload orphan (Codex review follow-up)** — a player deleted while the photo bytes were being written got a 200 and a permanently orphaned file (`photos/` is the one media dir the sweep doesn't reconcile); `set_player_photo` now reports whether the row existed and the router removes the file + 404s. 10 rounds of concurrent upload+delete left zero orphans. Full regression pass on a live server (clip import→save→PATCH→play/stop/next, hype flow, static assets, zero tracebacks). Declined from the same review: issue #28 (mpv-backend end-of-song contract holes) awaits owner sign-off per the change policy; #29 collects the nits. Second review round (CodeRabbit + a re-run of Codex on the final head): (5) the `sqlite3.IntegrityError` handler above mapped *every* constraint failure to "player deleted", so a CHECK/NOT NULL fault would have hidden behind a plausible 400 — now only `SQLITE_CONSTRAINT_FOREIGNKEY` is translated and everything else surfaces as a 500; (6) the same cascade delete can also land between the insert's COMMIT and the rename of the audio into place, which left `POST /api/clips` answering **201 with a null body** plus an orphaned `<id>.mp3` — the result is re-checked after the rename and reported as the same 400. Verified in-process (constraint classification across FK/CHECK/NOT NULL; a delete forced into the post-COMMIT window → 400, no file left behind). CodeRabbit's suggested fix for (6) — hold `item_lock` across insert→replace — was **not** taken: cascade deletes never acquire that lock (`delete_player` is a plain `DELETE FROM players`), and the new clip's id doesn't exist when the delete starts, so it would not have closed the window; its request for a regression test was declined per the repo's documented no-test-suite policy.

## Pending scope notes
- Multiple teams, >15 players (paged 5×3 grid), multiple clips per player (walkup tap / homerun long-press 600ms), touch drag-drop reorder — all implemented.
- Dev data note: ./data currently holds two demo clips for Bobby 'Rocket' Reyes (walkup 12s, homerun 8s, from "Me at the zoo") — delete via admin UI if unwanted.
