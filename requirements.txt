# fastapi + starlette are pinned as a PAIR. Starlette is pinned explicitly
# (not left to fastapi's floor) because two advisories hit BatterBox directly:
# GHSA-7f5h-v6xp-fcq8 (Range-header CPU DoS in FileResponse/StaticFiles — we
# serve the whole UI and all media that way) and GHSA-2c2j-9gv5-cj73 (large
# multipart uploads block the event loop — we accept 50MB imports). Both fixed
# in starlette >= 0.49.1. If bumping fastapi, keep starlette >= 0.49.1.
fastapi==0.140.0
starlette==1.3.1
uvicorn[standard]==0.34.0
python-multipart==0.0.31
yt-dlp==2026.7.4
gpiozero==2.0.1; sys_platform == "linux"
# gpiozero is frontend-only — lgpio is its pin backend on Pi 4 (needs swig +
# build-essential at pip-build time, installed and purged in the Dockerfile).
lgpio==0.2.2.0; sys_platform == "linux"
